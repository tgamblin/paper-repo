\newcommand{\ScalingPerfPlot}{%
  \begin{figure*}[!t]
    \resizebox{\textwidth}{!}{\input{scaling.pgf}}
    \caption{An evaluation of the scaling behavior of \splicespack{} when the
      number of potential splice candidates is increased.}
    \label{fig:splice-scaling}
    \Description{A bar chart demonstrating the overhead of introducing a number of splice candidates.}
  \end{figure*}%
}
